# .pls.yml
icons:
  pls: "󰱫" # Override the built-in glyph for 'pls'.
  sock: "󰓚" # Define new icon-glyph mapping.
entry_const:
  typ:
    fifo:
      icons:
        - fifo # Enable and use the built-in glyph for FIFO pipes.
    socket:
      icons:
        - sock # Enable and use a custom icon for sockets.
